import { formatAssetTypeLabel, formatPlatformLabel } from "@/lib/calendar/platform-options";
import type { ContentPillar } from "@/lib/calendar/types";
import type { UnscheduledIdea } from "@/lib/calendar/unscheduled-idea-types";
export function UnscheduledIdeaCard({idea,pillar,onEdit,onSchedule,onDelete}:{idea:UnscheduledIdea;pillar?:ContentPillar;onEdit:(id:string)=>void;onSchedule:(id:string)=>void;onDelete:(id:string)=>void}){const color=pillar?.color??"#94A3B8";return <article className="flex min-w-0 flex-col rounded-xl border border-[#d3e4fe] border-l-4 bg-white p-4 shadow-sm" style={{borderLeftColor:color}}><div className="flex items-start justify-between gap-3"><div className="min-w-0"><h3 className="break-words text-base font-extrabold">{idea.title}</h3><p className="mt-1 line-clamp-2 text-xs leading-5 text-[#657080]">{idea.coreTopic}</p></div><span className="shrink-0 rounded-full bg-[#e5eeff] px-2.5 py-1 text-[10px] font-bold text-[#0058bc]">{idea.source==="ai"?"AI":formatAssetTypeLabel(idea.source)}</span></div><dl className="mt-4 grid grid-cols-2 gap-3 text-xs"><Info label="Pillar" value={pillar?.name??"Unknown"}/><Info label="Objective" value={formatAssetTypeLabel(idea.objective)}/><Info label="Platform" value={idea.suggestedPlatform?formatPlatformLabel(idea.suggestedPlatform):"Not suggested"}/><Info label="Asset Type" value={idea.suggestedAssetType?formatAssetTypeLabel(idea.suggestedAssetType):"Not suggested"}/><Info label="Audience" value={idea.targetAudience}/><Info label="Updated" value={new Intl.DateTimeFormat("en-US",{dateStyle:"medium"}).format(new Date(idea.updatedAt))}/></dl><p className="mt-4 line-clamp-2 break-words text-sm leading-6 text-[#414755]">{idea.mainMessage}</p>{idea.notes&&<p className="mt-2 line-clamp-2 break-words text-xs text-[#657080]">{idea.notes}</p>}{idea.campaignId&&<p className="mt-2 break-all font-mono text-[10px] text-[#657080]">{idea.campaignId}</p>}<div className="mt-auto grid grid-cols-2 gap-2 pt-5 sm:flex sm:flex-wrap sm:justify-end"><button type="button" aria-label={`Delete ${idea.title}`} onClick={()=>onDelete(idea.id)} className="min-h-10 rounded-lg px-3 text-xs font-bold text-rose-600 outline-none hover:bg-rose-50 focus-visible:ring-2 focus-visible:ring-rose-600 sm:mr-auto">Delete</button><button type="button" aria-label={`Edit ${idea.title}`} onClick={()=>onEdit(idea.id)} className="min-h-10 rounded-lg border px-3 text-xs font-bold outline-none focus-visible:ring-2 focus-visible:ring-[#0058bc]">Edit</button><button type="button" aria-label={`Schedule ${idea.title}`} onClick={()=>onSchedule(idea.id)} className="col-span-2 min-h-10 rounded-lg bg-[#0058bc] px-3 text-xs font-bold text-white outline-none focus-visible:ring-2 focus-visible:ring-[#0058bc] sm:col-auto">Schedule</button></div></article>}
function Info({label,value}:{label:string;value:string}){return <div className="min-w-0"><dt className="font-bold uppercase tracking-[.08em] text-[#8b96a5]">{label}</dt><dd className="mt-1 break-words font-semibold">{value}</dd></div>}
